name: Keep Alive

on:
  schedule:
    - cron: '*/5 * * * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Curl health endpoint
        run: |
          curl -fsS https://seedtoplate-backend.onrender.com/api/health || (echo "health check failed" && exit 1)
